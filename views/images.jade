extends layout


block content
	div.gallery.js-masonry(data-masonry-options={"columnWidth": 10, "itemSelector": ".item" })
		- each image in images
			a(href="/i/" + image.split(".").shift())
				img.item(src="/images/" + tag + "/" + image)

